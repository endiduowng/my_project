<nav class="navbar navbar-expand-lg navbar-light bg-dark">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="nav justify-content-center mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="/">Home</a>
      </li>
    </ul>

    <%= form_for :term, url: search_path, method: :get do |form| %>
        <div class="input-group" style="width:300px;">
          <%= text_field_tag :term, params[:term], class: "form-control", placeholder: "Search" %>
          <%= submit_tag "Go", name: nil, class: "input-group-append btn btn-outline-primary" %>
        </div>
    <% end %>

    <ul class="navbar nav">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="javascript:void(0)" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= image_tag("default-avatar.png", :height=>'25px', :width=>'25px', :style => 'margin-right: 5px;border-radius: 50%') %>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
            <%= link_to "Profile", user_path(current_user), class: "dropdown-item" %>
            <a class="dropdown-item" href="">Settings</a>
            <div class="dropdown-divider"></div>
            <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <ul class="nav justify-content-center">
          <li class="nav-item">
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
        </ul>
      <% end %>
    </ul>
  </div>
</nav>
